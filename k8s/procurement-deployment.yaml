apiVersion: apps/v1
kind: Deployment
metadata:
  name: procurement-service
  labels:
    app: glosleep
    tier: backend
    service: procurement
spec:
  replicas: 2
  selector:
    matchLabels:
      app: procurement-service
  template:
    metadata:
      labels:
        app: procurement-service
        app.kubernetes.io/name: procurement-service
        app.kubernetes.io/part-of: glosleep-platform
    spec:
      containers:
      - name: procurement-service
        image: docker.io/anshumandash1506/glosleep-procurement-service:develop
        imagePullPolicy: Always
        ports:
        - containerPort: 3003
        env:
        - name: MONGODB_URI
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: MONGODB_PROCUREMENT_URI
        livenessProbe:
          httpGet:
            path: /health
            port: 3003
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3003
          initialDelaySeconds: 5
          periodSeconds: 5
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"